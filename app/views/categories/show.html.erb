<h1><%= @category.name %> has a total of <%= @category.articles.count %> articles</h1>

<div>
	<ul>
 <% @category.articles.each do |article| %>

 	<li><%= link_to article.title, article %></li>

 <% end %>
  </ul>
</div>